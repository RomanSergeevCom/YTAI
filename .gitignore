# macOS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
.venv*/

# Node
node_modules/

# Video files
*.MP4
*.mp4
*.mov
*.MOV
*.avi
*.mkv

# Audio files
*.wav
*.mp3

# Premiere Pro project files
*.prproj
*.prin
*.pek
*.cfa

# Premiere debug logs (contain .prproj snapshots)
scripts/05_editing/0500_uxp/logs/

# Testing project (video files ~6GB)
scripts/05_editing/999_testing_project/

# ML models (24GB — Ollama, HuggingFace, Whisper)
models/

# Dependency repos / clones (13GB)
github/

# Virtual environments (3GB)
environment/

# Config with API tokens
config/

# Bots data — binary inventory assets (kept on disk, backup via Drive)
Bots/*/inventory_photos/
# Bots data — generated prompt-packs (per-project, not for source repo)
Bots/*/_prompts/

# Sync layer — machine state (bisync workdir + mirror registry, per-machine)
scripts/11_sync/1101_mirror/state/

# yt_subtitles extension — native host runtime log (regenerated locally)
utils/video_downloader/native-host/ytdl_host.log

# Color layer — cube store (~670 MB, mirrored to Shared Drive YTAI) and machine state
scripts/15_color/1501_lut_library/store/
scripts/15_color/*/state/
