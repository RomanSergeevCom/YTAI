# HANDOFF · система «Ревью ката + ТЗ на монтаж» — 14.09.2026

**Запуск в новом чате:** `/review YTUVI02` (или любой код проекта) → скилл читает 5 файлов и вызывает `review.py`.
Этот файл — итог сессии, в которой система была построена. Живая документация: `README.md` стадии (машинный ранбук),
`docs/contracts.md` (схемы), KB 5.7 `/kb/review-cycle/` (человеческий ранбук), 5.8 `/kb/montage-tz/`, 5.6 (канон форматов).
Память: `reference_review_cycle_system` (индекс MEMORY.md).

## Что сделано (коммиты YTAI: 055744f → 298c66e → b90e1c3 → 761f424; KB: b4e12707)

1. **Перенос и заморозка.** `scripts/999_extra/review_timeline_audit/` → `scripts/05_editing/0509_review_cycle/` (git mv, история цела);
   незакоммиченные фиксы YTUVI02 12.09 (35 M + 7 ??) закоммичены. Старые раннеры — `_legacy_runners/`, 250-агентные воркфлоу — `cloud/_legacy/`.
2. **Карточка + профиль.** `proj_config.py` ищет `review_card.json` (YTAI_CARD / YTAI_PROJECT_DIR / вверх от cwd), даёт `WORK/MONT/CLOUD/MOCK`
   в проекте и `profile()` канала. 29 скриптов получили единый заголовок `from _bootstrap import P, W6, M, HERE, ROOT`.
   Сняты все дефолты чужого фильма (пути T7-Blue-2, главы YTUVI01, FROZEN-вкладка, id листа, стиль, shots_remote).
   `terms_catalog.py` = загрузчик JSON (`YTs/YTUVI/review_terms_base.json` + `review_terms.json` фильма).
   Профили `YTs/{YTUVI,YTCH,YTEVO}/review_profile.json`; YTCH — 25 паттернов чувствительности + правила структуры.
3. **`review.py`** — стейт-машина (скелет `999_extra/ytch_chain`): 31 стадия cut_review / 9 montage_tz, гейты по артефактам, resume,
   PAUSE/STOP (`~/.cache/<project>/`), TG-вехи, составная облачная стадия (pack → `cloud/CALL.txt` → collect → F/V/S), `edits`-гейт
   перед регенерацией вкладок, `ticket` → REVIEW_STATE.md, `docs` → таблица стадий в README и KB 5.7.
4. **Экономное облако.** `stages/route_candidates.py` (auto/cloud/drop, pymorphy3 + каталог + три чтения OCR/VLM/zoom),
   `stages/s3b_probe_vlm.py` (чужие следы, обрезка, zoom-перечит), `cloud/pack.py` (пакеты J ≤50 экранов текстом),
   `cloud/wf_judge.js` (J≤4 → F ‖ V → S; агент пишет `cloud/out/` сам), `cloud/collect.py` (покрытие, v8, `--emit-legacy`),
   `cloud/salvage.py` (`--cost`), `s8_apply_audit.py` под v8 + классовый фильтр + идемпотентность номеров.
5. **QA кодом.** `stages/doc_pdf_qc.py` (PDF вкладки: сироты заголовков, подписи, перечисления, длинные абзацы, картинки),
   `stages/preview_qc_local.py` (альфа-рамка, OCR-читаемость на 350 px, лучшая секунда, `--vlm`), `s14_polish_local` как единственная полировка.
6. **Режим montage_tz** (`montage/`): golden YTEVO02 — `montage.json` 0 расхождений, 28/28 мокапов побайтно; `segment_local` Qwen3-8B (~16 с/окно);
   `cloud/wf_structure_src.js` (1 агент структуры + 1 дизайн-ревью по контактному листу); карточка YTEVO02 создана.
7. **YTCH-линия** восстановлена из логов сессий (`shared/recover_from_session_log.py`; `examples/ytch12_v4/` 28 файлов, `examples/ytch11_v2/` 14):
   `shared/risk_registry.py`, `align.py`, `acts_compact.py`, `verdict_call.py` + `cloud/wf_verdict_doc.js`.
8. **Поверхности.** `shared/phone_brief.py` (390 КБ на YTUVI02, без JS, → TG), `producer_page.py`, `notes_sync.py` + `templates/notes-push.plist.j2`, `pravki_lib.py`.
9. **Memex.** `memex/push|pull|start|status|pause|resume|stop|watchdog` — код rsync'ом по путям репо (`VERSION` = git sha), данные в `~/YTAI_work/{CODE}/`.
10. **Входы и документация.** Скилл `/review`, раздел в `CLAUDE.md`, README стадии, KB 5.7/5.8 + правка 5.6 + `kb.json` + `gen_kb.py`, память.

## Проверено (0 токенов, если не сказано иначе)

- YTUVI02 (T7-Blue-2, данные мигрированы в `00_Setup/05_Review/`): регенерация поверхностей из старых находок = 46 ТЗ / 210 сегментов / mock 0 ошибок;
  **172/172 PNG и review JSON байт-в-байт со старым кодом**; s5 селфчек ALL OK.
- `route_candidates --eval`: **61/84** старых находок решаются локально, **0 ложных auto_confirm**, в облако 24 кандидата; пробы: «唐» (s058) пойман,
  «ВЫСОКВАЯ» (s031) → три чтения → auto; 12,8 с/экран на маке.
- `cloud/pack.py` на YTUVI02: 2 пакета J (48 экранов/90 КБ, 47/117 КБ) + V (14 кропов); `collect` — покрытие, pending, salvage, `--from-legacy` (0 новых ТЗ, 46 не тронуто).
- `doc_pdf_qc` на текущей вкладке YTUVI02: 33 страницы, 108 high (58 подписей «таймкод · что видно» отсутствуют, 41 длинная строка = lint s10) — честная картина дока,
  собранного до `s12 --apply`. `preview_qc_local`: 93/95 ok, 2 high (fix_tz09/35 срезаны) — подтверждено глазами.
- Облачный dry-run одного пакета J + скептик: run `wf_864cd569-366` — **2 агента, 291 995 токенов, 14:52 мин** на пакет из 48 экранов + скептик. Судья: 5 вердиктов (4 confirm, 1 refute — «ИСКУССТР» = титр закрыт рукой, верно), 8 новых находок по тексту без картинок (СИНЕТИКУ ×2, ВЫСОКВАЯ, «изогнутые линии роста» — признак синтетики, а не нагрева; порядок слов в названии 41-ФЗ; янтарь; $5 000 000 → $5 МЛН; УФ-свет через дефис), 4 need_frames, 4 факта на проверку, покрытие 48/48; скептик снял 0. `collect` слил в `audit_findings.json` (24 находки, 23 подтверждено); pending — J_02 и V (кропы): dry-run намеренно на одном пакете. Оценка на фильм: 4 J + F + V + S ≈ 7 агентов ≈ 1 M токенов (было 6,1–14,8 M за аудит + до 12,6 M догона).
- Memex автономно (`~/YTAI_work/YTUVI02`, старт 09:38 по Memex): старт 09:38 (время Memex); frames 1283 за 6 мин; OCR 95 экранов за 3 мин (= результат мака); transcript пропущен по артефакту (words.json залит push’ем); VLM идёт с 09:47 (~60–70 мин), дальше LLM (~20 мин) и probe (~50 мин); сторож жив, вехи в TG. Забрать: `review.py memex pull --project YTUVI02` → `run --from route` (кандидаты пересчитаются с полными пробами).

## 15.09 — полный цикл на YTUVI02 доведён до конца

- **Облако целиком:** 3 прогона, **7 агентов, 924 531 токен** (J_01+S 292 k · J_02+V+S 401 k · F+S 231 k), 55 находок, 41 подтверждена,
  11 опровергнуты судьёй (OCR-шум, титр под рукой), 3 снял скептик, факт-чек опроверг 6 утверждений с источниками (41-ФЗ, янтарь, «линии роста»,
  реквизиты лаборатории, «$ 30 300 000», адрес компании) и подтвердил 3. `s8_apply_audit`: новых ТЗ 0, стрелок 46 (все находки легли на экраны,
  где ТЗ уже были; текст существующих ТЗ при том же наборе классов не обновляется — см. хвосты).
- **Внешние поверхности проверены на тестовых ресурсах** (папка `_review_cycle_TEST_2026-09-14` в Drive-папке проекта YTUVI02: док
  `1R17Ez2U29s5UhMPcn9u4jOgFkIY48WnE0W7-2MWFaN4`, лист `1LQO9HYKcENULlBJFZC0W1xhe2U9FEQgOT11pR1xOzxA`, материалы `1HdfpZ1EATU19Tzn3r7GLQV4y3-52dISJ`;
  можно удалить): Drive 207 файлов с комментами (второй заход: 137/164 пропущены по md5), лист, вкладка «ТЗ монтажёру · v1» и навигатор,
  `verify` ALL PASS, `doc_pdf_qc` 41 страница / 71 картинка на месте, бриф ушёл в Telegram, страница продюсера. Гейт правок: `edits` снял
  правки с тестовой вкладки (0 расхождений), регенерация прошла через `doc_tz` без стопа. Рабочий док YTUVI02 не тронут.
- **Локальная полировка ТЗ работает без облака:** `polish_todo.py` (lint → `local_todo`/`r3_data`, с учётом меток блоков «✅ СДЕЛАТЬ ·») →
  `s14_polish_local` (41 строка за 78 с, 34 моделью, 7 детерминированно) → `merge_local_fixes.py` (guard_v7b, 29 из 31 ТЗ слиты).
- **Ошибки, найденные и починенные этим тестом:** `--from/--only` не перегоняли стадии с готовыми артефактами; `lint_long` читал не тот ключ
  (polish молча пропускался); обёртка `doc_tab_tz_v4` называла вкладку «· v4» (setdefault TZ_TAB) — теперь имя из карточки; `edits` требовал
  `tab_built_at` в карточке — теперь берёт момент записи вкладки из стейта; гейт правок привязан к той же вкладке и доку; внешние id можно
  подставлять окружением (`YTAI_DOC_ID`, `YTAI_MATERIALS_ID`, `YTAI_NOTES_SHEET_ID`, `YTAI_SHOTS_REMOTE`, `YTAI_NAV_TAB`) для тестов.
- **Регрессия:** `review.py selftest` (1 с, 0 токенов) — гонять после любой правки, до commit и `memex push`.
- **Нагрузка Memex подписана:** флаг `~/.cache/ytai/LOAD.json` (review.py на тяжёлых стадиях; `ytai_load start|stop|run` для любого джоба),
  TG «🔥 начинаю / 🧊 закончил», мониторинг Engelbart’а читает флаг (`memex-temp-check.sh` v3, `memex-resource-check.sh` v2). Причина 96 °C
  14.09 — QA иллюстраций RSC004 на том же стеке; фоновый нагрев — WindowServer 120 % (две консольные сессии с Aerial-обоями, тикет ENG).
- ✅ В конце сессии оригинальные `pravki/proj_material_ids.json` и `shots_ids.json` (настоящая папка Review_materials) возвращены из
  `~/YTUVI02_Review/work/montage/`; тестовые версии сохранены рядом как `*.TEST_2026-09-15.json`. Финальная регенерация (после `edits`,
  с полировкой): длинных строк ТЗ 41 → 7, мок-сборка 212 сегментов / 0 ошибок, verify ALL PASS.
- KB: Winston заметил, что в 9 страницах (включая новые 5.7/5.8) гейт-блок no-flash продублирован (маркированный + голый перед `</head>`) —
  на гейт не влияет, лишний запрос `site.css`; чинится одной строкой в `scripts/site/patch_site_chrome.py` (дедуп) отдельным коммитом.

## Не сделано / открыто (следующей сессии)

- **Живой док не писался**: стадии `drive/sheet/doc_tz/doc_nav/verify` на YTUVI02 в новой системе не гонялись (Google-запись; вкладку через API не удалить).
  Первый реальный прогон — на следующем кате: `edits` → `run --from format_tz`. Код этих стадий не менялся, кроме FROZEN/tab_title/notes_sheet_id из карточки.
- `s10_format_tz`, `review_page`, `doc_tab_review_v1` читают `audit_findings_v6.json` — `collect --emit-legacy` вызывается автоматически после collect; перевести их на v8 — задача на потом.
- `s8_apply_audit`: находка v8 на экране с уже существующей ТЗ при том же наборе классов не обновляет её текст (fix_text/why из судьи теряются) — научить сливать улучшенный `fix_text` в `parts.do` без перенумерации.
- `polish`: 2 ТЗ из 31 заблокированы guard’ом (таймкод не в начале строки) — остаются в исходном виде; смотреть `logs/polish_merge.log`.
- Полный прогон проб (95 экранов) и повторный `route` на YTUVI02 — после Memex-прогона (`memex pull` → `run --from route`).
- 11 пропусков recall (метки на карте, ИИ-фото, en-dash, мелкий слоган, смысловые mismatch) — это слой судьи J; проверить на реальном облачном прогоне всего фильма.
- Профиль YTCH: паттерны `debts`/`harassment`/`age_dates` шумят на живой речи — откалибровать на транскрипте YTCH12 v4, когда смонтирован T7-Beige.
- `review_page.py` хардкодит шапку YTUVI02 (обойдено в `producer_page`) — параметризовать.
- Архив: `~/Downloads/YTUVI01_Sonya_cut/work/`, `~/YTUVI02_Review/work/*.py`, Memex `~/YTAI/scripts/999_extra/review_timeline_audit`, `~/YTUVI02_Review/work/v6/*.py` — устарели, удалить руками после проверки (медиа не трогать).
- launchd `ae.rya.ytuvi01-notes-push` / `ytch11` / `ytch12` — старые пути; перегенерить `notes_sync.py install-autopush` или bootout.
- KB задеплоен (Winston, GH Actions run 34810623474): `https://yt.rya.ae/kb/review-cycle/` и `/kb/montage-tz/` — 200, тайлы 5.7/5.8 на хабе.
- Memory-грабля: карточка YTUVI02 `ocr_anchors` пуста — селфчек OCR по якорям пропускается; заполнить 2–3 фразы.

## Как продолжить на следующем кате (любой канал)

```bash
R=~/YTAI/scripts/05_editing/0509_review_cycle/review.py
python3 $R init  --project /Volumes/…/YTXX00_Name --channel YTUVI --mode cut_review --cut gdrive:…/cut.mp4
#   заполнить карточку: chapters/ch_name, film, doc_id, materials_id, project_folder_id, sprint_folder_id, ocr_anchors, notes
python3 $R memex --project P push --with-cut   # или без --with-cut, если cut_drive указан
python3 $R memex --project P start             # автономно, вехи в TG; статус: memex status; пауза: memex pause
python3 $R memex --project P pull && python3 $R run --project P --until route
python3 $R cloud --project P judge --print-call   # → Workflow({...}) ОДИН раз → cloud collect --run <id>
python3 $R resume --project P                     # apply → … → doc → verify → бриф в TG
python3 $R edits --project P                      # после правок Романа в доке — перед регенерацией
```
