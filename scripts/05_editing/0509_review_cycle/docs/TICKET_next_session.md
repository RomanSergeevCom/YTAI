# ТИКЕТ · практика и правки системы «Ревью ката + ТЗ на монтаж» — новая сессия

> ⚠️ **21.09.2026 — сначала прочитать `docs/TICKET_feedback_format.md`:** переделать формат
> обратной связи (вкладка «Сверка v4 → v5» нечитаема), там же хэндофф по YTCH12 v5,
> шесть незакоммиченных правок системы и пять новых системных находок.

**Запуск в новом чате:** `/review` (скилл сам читает README, contracts, карточку и профиль) или просто
«ревью YTUVI02 …» / «измени в системе ревью …». Роман будет **практиковать** систему на реальных катах и
**просить менять** правила, форматы и стадии. Работать локально, act-then-report, статусы писать по ходу
любой операции длиннее минуты (см. «Правила сессии»).

## 0. Состояние на 15.09.2026

- Система собрана и проверена целиком на YTUVI02: коммит `ff03ac5` в `~/YTAI` (папка `scripts/05_editing/0509_review_cycle/`),
  KB 5.7/5.8 задеплоены, инфографика `docs/scheme_2026-09-14.html` (артефакт v5, копия в TG).
- Что было сделано и найдено — `docs/HANDOFF_2026-09-14.md` (там же список хвостов). Не переписывать, дополнять.
- YTUVI02 (T7-Blue-2): карточка `00_Setup/05_Review/review_card.json`, `pravki/pravki_v2.json` = 46 ТЗ (полированные тексты
  через `tz_overrides.json`), `work/v1/audit_findings.json` = 55 находок нового облака (41 подтверждена), `cloud/` все пакеты done.
  **Рабочий док «ТЗ монтажёру · v1» YTUVI02 не тронут.** Все внешние поверхности гонялись на тестовых ресурсах в Drive-папке
  проекта `_review_cycle_TEST_2026-09-14` (док `1R17Ez2U29s5UhMPcn9u4jOgFkIY48WnE0W7-2MWFaN4`, лист `1LQO9HYKcENULlBJFZC0W1xhe2U9FEQgOT11pR1xOzxA`,
  материалы `1HdfpZ1EATU19Tzn3r7GLQV4y3-52dISJ`) — можно удалить; `pravki/proj_material_ids.json` и `shots_ids.json` возвращены на
  настоящую папку (тестовые копии `*.TEST_2026-09-15.json`).
- Memex: код стадии залит (`memex push`, VERSION = sha), данные YTUVI02 в `~/YTAI_work/YTUVI02/`, флаг нагрузки и сторож работают.
- В `examples/ytevo02/*` и `shared/notes_sync.py` есть незакоммиченные правки **другой сессии (YTEVO02)** — не трогать, не откатывать.

## 1. Сценарии практики (что Роман скорее всего попросит)

| Роман говорит | команда | чего ждать |
|---|---|---|
| «пришёл кат YTXX00, разбери» | `review.py init --project <path|CODE> --channel <CH> --mode cut_review --cut gdrive:…` → заполнить карточку (chapters/ch_name, film, doc_id, materials_id, project_folder_id, sprint_folder_id, ocr_anchors, notes) → `card check` → `memex push [--with-cut]` → `memex start` | TG «🔥 начинаю…», через 2–3 ч «🏁»; статус `memex status` |
| «что там на Memex» / пауза | `memex status` · `memex pause` / `resume` / `stop` | таблица стадий, дрейф VERSION |
| «локальный разбор готов» | `memex pull` → `run --until route` | candidates.json, сводка auto/cloud/drop |
| «запускай облако» | `cloud judge --print-call` → выполнить `Workflow({...})` ОДИН раз → `cloud collect --run <id>` → `resume` (при pending — повтор `judge --print-call`, будут F/V/S) | ≈7 агентов, ≈1 M токенов на фильм; `cloud cost --session <id>` |
| «примени к доку» (первый раз для фильма) | `resume` доводит до `doc_tz/doc_nav/verify/doc_qc/phone_brief` | verify ALL PASS, бриф в TG |
| «я поправил док, пересобери» | `edits` → `run --from format_tz` (или `--from polish`, `--from render`, `--from doc_tz`) | без облака; гейт не пустит без `edits` |
| «пришли бриф на телефон» | `run --only phone_brief` | файл ≤1 МБ в TG |
| «YTEVO: лист из исходников» | `init --mode montage_tz` → `run` → `cloud structure --print-call` → Workflow → `resume` | montage.json, мокапы, standalone |
| «сколько стоило / что упало» | `cloud cost` · `cloud salvage --latest` | таблица прогонов; спасённые пакеты |
| «проверь систему» | `review.py selftest` | 1 с, ИТОГ OK |

Все команды: `python3 ~/YTAI/scripts/05_editing/0509_review_cycle/review.py <cmd> --project <path|CODE>`.
Длинные стадии на маке — фоном (`nohup caffeinate -dims … &`, лог `05_Review/logs/<stage>.log`), Memex — `memex start`.
⚠️ Один прогон на проект: пока идёт `run`, второй `run`/`stage start` откажет (`review.pid` в `~/.cache/<project>/`).

## 2. Хочу изменить … → правлю здесь (и только здесь)

| что | где | после правки |
|---|---|---|
| правила канала: валюта, язык титров, что ошибка, латиница-исключения, чувствительные темы, правила структуры | `YTs/{CH}/review_profile.json` (`rules_text`, `tz_classes`, `latin_whitelist`, `sensitivity`, `structure_rules`) | ничего не пересобирать; на следующий `route/pack` подхватится |
| термины, места, каноны названий, путаемые пары | `YTs/YTUVI/review_terms_base.json`; у фильма `review_terms.json` | `run --from terms` |
| главы, подглавы, перечисления, якоря, ссылки на док/Drive, грабли | `review_card.json` фильма | `card check`; `run --from render` при смене глав |
| как судит облако (промпты J/F/V/S, классы, «не ошибка») | `cloud/wf_judge.js` | `node --check`; `selftest` |
| пороги маршрутизации (auto/cloud/drop) | константы в шапке `stages/route_candidates.py`; `--eval` на YTUVI02 | цель: 0 ложных auto |
| порядок/набор стадий, хосты, гейты | `STAGES_CUT` / `STAGES_MONTAGE` + `STAGE_DESC` в `review.py` | `review.py docs` (таблица в README и KB) |
| вид вкладки ТЗ / навигатора / листа / таймлайна (канон 5.6) | `stages/doc_tab_tz_v3.py` (+`_verify`), `doc_tab_review_v1.py`, `tz_sheet.py`, `make_review_v6.py` | `run --from doc_tz` / `--from review_json` |
| графика: плашки, карты, стрелки, карточки глав, стиль | `stages/make_infographics_v6.py` + `style` в профиле | `run --from render` |
| бриф для телефона / страница продюсера / заметки | `shared/phone_brief.py`, `producer_page.py`, `notes_sync.py` | `run --only phone_brief` |
| QA дока / превью (критерии, пороги) | `stages/doc_pdf_qc.py`, `preview_qc_local.py` | `run --only doc_qc` |
| полировка текста ТЗ (модель, правила, страж) | `stages/s14_polish_local.py`, `polish_todo.py`, `merge_local_fixes.py`, `guard_v7b.py`, `listify_rules_v2.md` | `run --from polish` |
| Memex-раннер, сторож, флаг нагрузки | `memex/*.sh`, `LOAD_FLAG`/`HEAVY` в `review.py`, `999_extra/bin/ytai_load` | `memex push` |
| формат обратной связи / сверки версий (что стало с прошлым ТЗ) | `shared/feedback_model.py`, `feedback_view.py`, `feedback_page.py`, `stages/doc_tab_feedback_v1.py`, `docs/feedback_v1.md` | `run --from feedback` |

Порядок любой правки кода: правка в репо → `review.py selftest` → commit → `memex push` (если трогали стадии Memex).
Скрипты **никогда** не правятся «под проект» внутри сессии и не копируются в проект; разовое — в scratchpad.

## 3. Правила сессии (Роман просил)

- **Статусы**: перед любой операцией длиннее минуты написать, что запускается и сколько ждать; во время ожидания — проверять и
  писать прогресс (уведомления фоновых задач могут не прийти — опрашивать самому); «молчание = завис» недопустимо.
- Гигиена токенов: кадры/скриншоты только `shared/shot.py` (≤1280 px; имена скриншотов macOS содержат узкий пробел — искать через `ls -t ~/Desktop/Screenshot*`),
  JSON > 50 КБ только `shared/peek.py`, читать 5 файлов входа, не больше.
- Облако: один Workflow на стадию, аргументы из `--print-call`, сразу `cloud collect`; повтор только pending; QA-стадий агентами не добавлять.
- Внешние записи (док, лист, Drive): в рабочий док только после явного «примени»; тест — на тестовых ресурсах через окружение
  `YTAI_DOC_ID / YTAI_MATERIALS_ID / YTAI_NOTES_SHEET_ID / YTAI_SHOTS_REMOTE / YTAI_NAV_TAB`.
- Memex: тяжёлое — только там, флаг нагрузки ставится сам; чужие тяжёлые прогоны заявлять `ytai_load start "…" <мин>`.
- Форматы для монтажёра (6 слоёв, ОДНА таблица, блоки ❌/✅/📋/📍/📚/🎬/💬) — канон KB 5.6; менять только по просьбе Романа.

## 4. Открытые хвосты (кандидаты на «измени»)

- `s8_apply_audit`: находка v8 на экране с существующей ТЗ при том же наборе классов не обновляет её текст (улучшенный fix_text судьи теряется).
- `s10_format_tz`, `review_page`, `doc_tab_review_v1` читают легаси `audit_findings_v6.json` (авто-конвертация `collect --emit-legacy`); перевести на v8.
- `doc_pdf_qc` после полировки: 17 high (9 «перечисление», 7 длинных строк из 2 ТЗ, заблокированных guard’ом, 1 подпись) — либо ослабить критерий « / » в цитатах, либо доразбить вручную.
- `preview_qc_local`: 2 high (fix_tz09/35 срезаны справа) — поправить кропы в `pravki/previews_v7.json` или в `s12`.
- Профиль YTCH: паттерны `debts`/`harassment`/`age_dates` шумят — калибровать на транскрипте YTCH12 v4 (T7-Beige).
- **Номерные фикстуры YTUVI01 привязаны и к версии ката** (22.09.2026, YTUVI01 v2, исправлено): `ADD_MAT`
  в `s10_format_tz` и `_ATTACH_FILES` в `s12_doc_previews` писались под номера ТЗ круга v1. На кате v2 список
  вырос с 55 до 58 ТЗ, номера уехали — и карточка прошлого круга молча приезжала к чужому ТЗ: ТЗ-02
  «UVi | ZUAETDI» получил карту Могока, ТЗ-11 «Mozambique» — плашку «ШЁЛК», ТЗ-25 «БИРМАНСКИЙ РУБИН» — три
  плашки про high jewelry, новые ТЗ-56/57 — драфты сертификата и пресс-релиза. Теперь гейт ещё и по
  `P.CUT_VERSION == 'v1'`; 14 чужих картинок снято с pravki YTUVI01 руками. **Кандидат на доработку:**
  вместо привязки к версии — гард по заголовку, как `_title` у `tz_overrides` (фикстура прилипает, только
  если ТЗ на этой позиции называется так же, как когда её писали).
- **`AUTO_SAME_LEN` — auto-опечатка только при равной длине** (22.09.2026, YTUVI01 v2): из 41 auto_confirm
  **20 были ложными** — словарь «правил» имена и термины (`Пьин→Пьи`, `Чаук→Чак`, `Яунг→Янг` — топонимы карты
  Могока; `Моосу→Мосу`; `РОБЕРТС→РОБЕРТАС`; `БЕЗЕЛЬ→БЕНЗЕЛЬ`, `КУЛЕТА→КУПЛЕТА` — огранка). Закономерность:
  верные авто — всегда замена/перестановка букв (OCR путает начертания, длина та же), ложные — всегда вставка
  или удаление. Порог ратио тут не помогает: `РОБЕРТС→РОБЕРТАС` = 0,933, как у верного `ВЫСОКВАЯ→ВЫСОКАЯ`.
  Цена на эталоне YTUVI02 — 2 верные правки уходят к судье вместо авто (`СИНЕТИКУ`, `ВЫСОКВАЯ`), golden пересnят.
  Следующий шаг, если захочется вернуть эти двое: отличать имя от нарицательного (pymorphy `Name`/`Surn`/`Geox`)
  или помечать строки, которые VLM описал как карту.
- **Memex — 16 ГБ, два фильма сразу не помещаются** (21.09.2026, YTUVI01 v2): пока YTCH12 доделывал автономную
  цепочку (`polish`/`render`, Qwen3-8B), `vlm` YTUVI01 падал `[METAL] Insufficient Memory` — MLX держит ~10,4 ГБ
  wired, на кадр 1920×1080 в vision-tower не остаётся. `s3_vlm` теперь переживает это сам (`mx.clear_cache()`
  между экранами + повтор на 1280 px + явный `skipped`), но **очередь `wait_load` дыру не закрывает**: чужой
  флаг снимается на нетяжёлой стадии (у YTCH12 это был `cloud`), второй фильм стартует и встречает соседа уже
  на `acts`/`polish`. Кандидат на правку: держать флаг на весь `run --host memex`, а не по стадиям.
- `review_page.py` (страница экранов) хардкодов больше нет, но стиль старый; при желании объединить с `producer_page`.
- KB: дубль гейт-блока no-flash в 9 страницах (включая 5.7/5.8) — одна строка в `scripts/site/patch_site_chrome.py` (Winston предлагал).
- Старые копии: `~/Downloads/YTUVI01_Sonya_cut/work/`, `~/YTUVI02_Review/work/*.py`, Memex `~/YTAI/scripts/999_extra/review_timeline_audit` — удалить руками после проверки; launchd `ae.rya.ytuvi01/ytch11/ytch12-notes-push` → bootout или перегенерить.
- Карточка YTUVI02: `ocr_anchors` пусты (селфчек OCR по якорям пропускается).

## 4c. YTUVI02 ревью v3 (16.09.2026) — что сделано и хвосты

Сделано (всё локально, без облачных агентов): вкладки «ТЗ монтажёру · v3» (100 ТЗ, 6 колонок с «Говорит») и «Ревью v3 по видео»,
verify ALL PASS; 32 заметки 007–038 → ТЗ-79…100 и правки в ТЗ-55/57/59/61/66/68/71/73/76/77; 8 книжных сканов заменены или сняты
(ТЗ-63 дубль, ТЗ-64 в вырезанном куске → rejected); ссылки на сами файлы (`shared/drive_links.py`); ТЗ-75/76 и мусор YTUVI01
(`ADD_MAT`, `_ATTACH_FILES`) вычищены; номера ТЗ держатся (`kbv_group`), `ensure_shots` перед записью вкладки.

Хвосты:
- KB 5.6 (`/kb/review-timeline/`): описать 6 колонок и «Говорит» — текст поправить в `~/RYA/yt-rya-ae/web/kb/review-timeline/`, деплой — Winston.
- `_KB/book_map.json` и `gem_kb/find_originals.py` по умолчанию: пути `01_SSEF/01_Book` устарели (сканы в `01_ScanBook-SSEF`); `gem_kb/` не в git.
- Лист «ТЗ монтажёру» (`tz_sheet.py`): колонку «Говорит» после «Что сделать» не добавляли.
- Ширины v3 `[30, 56, …]`: «ТЗ-60» и «14:24–14:27» переносятся в узких колонках (решение Романа 16.09; если мешает — 34/60 за счёт «Говорит»).
- doc_qc v3: 19 high — 16 в старых ТЗ-01…45 («/» и длинные абзацы: в колонке 176 pt URL переносится и склеивает строки),
  3 «нет подписи» у ТЗ-51/60 — подпись второй картинки уходит на следующую страницу PDF.
- golden пересъёмлен на данных v3 (16.09, 337 выходов), selftest OK. Грабли: `producer_page` берёт картинку из `work/v1/err_frames`, которых в песочнице нет, — при следующем расхождении смотреть сюда, а не в код.
- Решения Роману: ТЗ-79 (доснять рефрактометр — своей съёмки нет), ТЗ-95 (GIA Report Check снять вручную), ТЗ-100 (снять реальные бирки UVI).

## 4b. Визуал из базы — что доделать (16.09.2026, YTUVI02)

- `kb_visuals propose` собрал `work/v1/kb_visuals/proposals.json` — 14 вставок (9 книг, 5 журналов), проверены локальной Qwen2.5-VL.
  **Влить в `picks.json` → `cards && apply` → `run --from format_tz --until doc_qc`.** Контактный лист: `sheet_proposals_p1.jpg`.
- Нарезка фрагментов своих клипов в Drive (`upload_own_clips`, ключ `--no-clips`) и плашка источника на макете (`stamp_source`)
  написаны, но ещё ни разу не прогонялись живьём — первый `apply` покажет.
- Книги: источник — разметка `halves_manifest.json`, не индекс (в индексе фигур из книг нет). `photo_plate` честнее VLM-поля `form`.
- Осталось незакрытым: из 66 пустых битов черновик закрывает 14; остальное упирается в то, что 407 из 542 картинок базы — полосы
  с текстом. Резервы: свои клипы (160 совпадений), нарезка фигур из книжных страниц, снижение `--min-q` до 3 с ручным отбором.
- doc_qc YTUVI02: 14 high — все старые ТЗ-01…45 (9 «/», 4 длинных абзаца, 1 подпись).

## 4a. Из ревью иллюстраций DBeaver (RSC004, 15.09) — кандидаты на «измени»

Роман смотрел QA-столбец в гугл-доке RSC004 и сказал: «просто и понятно». Что из этого переносится на ТЗ монтажёру
по катам. Канон 5.6 по-прежнему меняется только по его «измени», но п. 1 он уже прямо сформулировал.

| # | что | Роман сказал / почему | где править | как проверить |
|---|---|---|---|---|
| 1 | **Цвета «было → стало»:** «было» — красным жирным, **без зачёркивания**; «стало» — **зелёным** жирным | 15.09: «красным — то, что исправляем, зелёным — как надо»; «зачем зачёркивать? тяжело же прочитать». Сейчас в коде иначе: «стало» красным на розовом, «было» зачёркнуто | `stages/doc_tab_tz_v3.py`, блок `for ty in r['typo']` (RED / PINK / strikethrough) + строка шапки «Как читать» («изменённые знаки выделены красным»); `phone_brief` / `producer_page`, если печатают опечатки | `run --from doc_tz` на тестовом доке → `verify` + глазами одна ТЗ с опечаткой |
| 2 | Те же цвета не только в опечатках: в ❌ СЕЙЧАС ключевой кусок — красным, в ✅ СДЕЛАТЬ — зелёным | в RSC004 похвалил именно это: сразу видно, что меняется | новое поле `parts.hl` `{now: [фрагмент], do: [фрагмент]}` (contracts §5) + стиль в `doc_tab_tz_v3.py`; откуда брать фрагменты (судья или полировка) — решить | guard: фрагмент дословно есть в строке; `verify` проверяет цвета |
| 3 | Шапка ТЗ ≤ 6 слов; ❌ — одно предложение «что на экране», без оценок; ✅ — начинается с глагола и даёт точную замену | стиль, который Роману нравится | 3 правила в `stages/listify_rules_v2.md` + `lint` | `doc_qc`: шапка > 6 слов = low |
| 4 | Под ТЗ — крупный кроп именно места ошибки (bbox из OCR/probe, ×2–3, рамка с номером ТЗ), а не только весь кадр | в колонке ~225 pt полный кадр с мелким титром не читается | сверить с превью `s12` / `previews_v7`; если там только полный кадр — добавить кроп | `preview_qc_local`: титр на кропе читается |
| 5 | Судья: **устаревшая версия логотипа бренда** — класс `fact` (сейчас в `foreign_trace` только «чужой логотип»). Пример: у Google Cloud с 2025 градиентное облако; плоское четырёхцветное — старое | в RSC004 нашлось на 3 кадрах; эталонный SVG в папке проекта тоже был старым | `cloud/wf_judge.js` (`CLASSES`, чек-лист F) | `node --check`; `selftest` |

Грабли гугл-дока, проверенные на RSC004 (частично здесь уже учтены):
- Индексы Docs API — в UTF-16 (📋 📍 📚 🎬 💬 = 2 единицы). `index_of` уже считает через `u16()` — новые стили считать только через него.
- Вставляя текст в ячейку, явно задавать `bold / italic / strikethrough / foregroundColor` на весь кусок — иначе наследуется стиль соседней ячейки (в RSC004 так ячейка стала курсивом).
- После записи — посимвольная сверка текста каждой ячейки с ожидаемым и проверка цветов, а не только «ячейка не пустая».

Калибровка локальных моделей (RSC004, 45 кадров, Memex): Qwen2.5-VL-7B, Qwen3-VL-8B (скачана в `~/YTAI/models/huggingface`) и
Qwen3-8B как сверщик нашли 3 из 17 подтверждённых блокеров, остальное — общие или выдуманные замечания. Все опечатки на экране
нашёл Apple Vision OCR, тайминг — whisper. Это подтверждает правило «VLM/LLM — признаки, не вердикты»; Qwen3-VL вместо
Qwen2.5-VL в `s3_vlm` без отдельного `route_candidates --eval` не ставить.

## 5. Где всё лежит

Код `~/YTAI/scripts/05_editing/0509_review_cycle/` (README · docs/contracts.md · docs/HANDOFF_2026-09-14.md · docs/scheme_2026-09-14.html · этот тикет) ·
скилл `~/YTAI/.claude/skills/review/SKILL.md` · раздел в `CLAUDE.md` · профили `YTs/{CH}/review_profile.json` · KB 5.7 https://yt.rya.ae/kb/review-cycle/ ,
5.8 https://yt.rya.ae/kb/montage-tz/ , 5.6 https://yt.rya.ae/kb/review-timeline/ · память `reference_review_cycle_system` (+ `reference_memex_mlx_stack` про нагрузку) ·
проект: `{project}/00_Setup/05_Review/REVIEW_STATE.md` (генерится `review.py ticket`) · чаты-источники: см. HANDOFF «Где лежит».
