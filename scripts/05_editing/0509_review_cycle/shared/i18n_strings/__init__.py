# -*- coding: utf-8 -*-
"""Таблицы строк поверхностей ревью по владельцам: <owner>.py → STRINGS = {'<owner>.key': {'ru': …, 'en': …}}.

Загружает их shared/i18n.py (все модули этой папки, по пути файла). Правила:
- ключи файла начинаются с «<имя файла>.» (core.py → 'core.*'); дубль ключа между файлами = ImportError;
- 'ru' — байт-в-байт прежний литерал из кода (RU-каналы обязаны остаться идентичными, см. shared/golden.py);
- 'en' пишется для всего, что видит англоязычный монтажёр; нет 'en' при LANG=en → KeyError (без тихой утечки RU).
"""
