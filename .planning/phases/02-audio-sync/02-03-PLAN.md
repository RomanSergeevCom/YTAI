---
phase: 02-audio-sync
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - scripts/01_prepare/0104_sync_audio_nested/0104_sync_audio_nested.py
autonomous: false
requirements:
  - AUD-06

must_haves:
  truths:
    - "Script runs to completion on reference project apartment scene (40 clips) without errors"
    - "Each clip has {clip}_AUDIO.wav under Transcription/per_clip/apartment/{clip}/"
    - "Each clip has {clip}_TX01.wav and {clip}_TX02.wav under Source/Audio/"
    - "Sync report shows delta in frames; all clips ≤1F"
    - "apartment_ingest.json exists in Source/Setup/ with A1/A2/A3 tracks for all clips"
  artifacts:
    - path: "01_Media/Source/Setup/apartment_ingest.json"
      provides: "Per-scene ingest metadata for apartment scene"
      contains: "TX01_SYNC"
    - path: "01_Media/Source/Audio/"
      provides: "Synced TX WAV files for apartment scene clips"
  key_links:
    - from: "0104_sync_audio_nested.py --scene apartment"
      to: "01_Media/Source/Audio/{clip}_TX01.wav"
      via: "process_scene -> process_clip -> trim_tx_to_clip"
      pattern: "_TX01.wav"
---

<objective>
Run the sync script on the real reference project (apartment scene, 40 clips) to verify end-to-end correctness. Fix any issues discovered during the live run. This is the integration test that validates AUD-06 (sync delta in frames).

Purpose: Prove the script works on real audio data, not just synthetic sine waves. The apartment scene (40 clips) is the smallest non-trivial scene in the reference project.
Output: Verified sync on apartment scene, any bugfixes applied, human confirmation of sync quality.
</objective>

<execution_context>
@/Users/romansergeev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/romansergeev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-audio-sync/02-RESEARCH.md
@.planning/phases/02-audio-sync/02-01-SUMMARY.md
@.planning/phases/02-audio-sync/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dry-run on apartment scene + fix any issues</name>
  <files>
    scripts/01_prepare/0104_sync_audio_nested/0104_sync_audio_nested.py
  </files>
  <read_first>
    scripts/01_prepare/0104_sync_audio_nested/0104_sync_audio_nested.py,
    .planning/phases/02-audio-sync/02-02-SUMMARY.md
  </read_first>
  <action>
    1. Activate venv: `source ~/YTAI/environment/.venv_transcribe/bin/activate`

    2. Run dry-run first to verify scene detection and clip counting:
       ```bash
       python scripts/01_prepare/0104_sync_audio_nested/0104_sync_audio_nested.py \
         --project "/Volumes/RYA T7 Black/YTCR_1_Arty_Dzis" \
         --scene apartment \
         --dry-run
       ```
       Expected output: "Scene: apartment (40 clips)" with list of planned operations.

    3. If dry-run succeeds, run the real sync on apartment:
       ```bash
       python scripts/01_prepare/0104_sync_audio_nested/0104_sync_audio_nested.py \
         --project "/Volumes/RYA T7 Black/YTCR_1_Arty_Dzis" \
         --scene apartment
       ```

    4. Inspect the output:
       - Check that `01_Media/Source/Audio/` contains `{clip}_TX01.wav` and `{clip}_TX02.wav` for each apartment clip
       - Check that `01_Media/Source/Transcription/per_clip/apartment/{clip}/{clip}_AUDIO.wav` exists for each clip
       - Check that `01_Media/Source/Setup/apartment_ingest.json` exists and contains valid JSON with A1/A2/A3 tracks
       - Check sync report: all clips should show ≤1F delta

    5. If any errors occur (import errors, ffmpeg failures, OOM, wrong paths, etc.):
       - Fix the issue in 0104_sync_audio_nested.py
       - Re-run on apartment scene
       - Repeat until clean completion

    6. After successful run, verify unit tests still pass:
       ```bash
       python3 -m pytest scripts/01_prepare/0104_sync_audio_nested/tests/ -x -q
       ```
  </action>
  <verify>
    <automated>cd /Users/romansergeev/YTAI && source ~/YTAI/environment/.venv_transcribe/bin/activate && python3 -m pytest scripts/01_prepare/0104_sync_audio_nested/tests/ -x -q 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - Script completes without Python exceptions on apartment scene
    - `ls "/Volumes/RYA T7 Black/YTCR_1_Arty_Dzis/01_Media/Source/Setup/apartment_ingest.json"` exits 0
    - `python3 -c "import json; d=json.load(open('/Volumes/RYA T7 Black/YTCR_1_Arty_Dzis/01_Media/Source/Setup/apartment_ingest.json')); print(len(d['clips']), d['clips'][0]['tracks']['A2']['type'])"` prints a number and "TX01_SYNC"
    - `python3 -m pytest scripts/01_prepare/0104_sync_audio_nested/tests/ -x -q` exits 0
  </acceptance_criteria>
  <done>Script ran to completion on apartment scene. Ingest.json generated. Unit tests still pass. Any bugfixes committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of sync quality on apartment scene</name>
  <files>none</files>
  <action>
    Human reviews the sync output from Task 1. No code changes — this is a quality gate.
    The executor presents the sync report and ingest.json for the user to inspect.
  </action>
  <what-built>
    The 0104_sync_audio_nested.py script processed the apartment scene (40 clips) on the reference project. Each clip now has:
    - Camera audio WAV in Transcription/per_clip/apartment/{clip}/
    - TX01 synced WAV in Source/Audio/
    - TX02 synced WAV in Source/Audio/
    - apartment_ingest.json with A1/A2/A3 tracks
  </what-built>
  <how-to-verify>
    1. Open the sync report output from Task 1 — check that all 40 clips show ≤1F delta for both TX01 and TX02
    2. Spot-check 2-3 clips in a DAW or media player:
       - Play the camera audio and TX01 side by side — verify they are aligned (clap or speech onset matches)
       - Play the camera audio and TX02 side by side — same check
    3. Open `apartment_ingest.json` — verify it has 40 clips, each with A1/A2/A3 tracks
    4. Check if any clips show LOW_CONF — if so, verify those are clips with minimal speech (expected)
    5. If satisfied: type "approved"
    6. If issues found: describe what's wrong (e.g., "TX01 is 2 frames early on C5089", "missing TX02 for 3 clips")
  </how-to-verify>
  <verify>Human types "approved" or describes issues</verify>
  <done>Human confirmed sync quality is acceptable on apartment scene</done>
  <resume-signal>Type "approved" to proceed, or describe sync quality issues</resume-signal>
</task>

</tasks>

<verification>
- apartment_ingest.json exists and has correct structure
- All apartment clips have TX01 and TX02 WAV files (or LOW_CONF documented)
- Sync report shows ≤1F delta for all clips
- Human verified sync quality on spot-checked clips
</verification>

<success_criteria>
- Script processes 40 apartment clips without errors
- Sync accuracy ≤1F for all clips (or LOW_CONF for silent/short clips)
- Human approves sync quality after spot-check
- Unit tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/02-audio-sync/02-03-SUMMARY.md`
</output>
