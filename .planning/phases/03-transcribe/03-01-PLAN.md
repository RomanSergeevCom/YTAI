---
phase: 03-transcribe
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - scripts/02_transcribe/0201_transcribe_nested/__init__.py
  - scripts/02_transcribe/0201_transcribe_nested/0201_transcribe_nested.py
  - scripts/02_transcribe/0201_transcribe_nested/merge_transcripts.py
  - scripts/02_transcribe/0201_transcribe_nested/tests/__init__.py
  - scripts/02_transcribe/0201_transcribe_nested/tests/conftest.py
  - scripts/02_transcribe/0201_transcribe_nested/tests/test_0201.py
  - scripts/02_transcribe/0201_transcribe_nested/tests/test_merge.py
autonomous: true
requirements: [TRN-01, TRN-02, TRN-03]

must_haves:
  truths:
    - "detect_scenes() finds all scene subfolders under Source/Video/ without requiring numeric prefixes"
    - "transcribe_scene() constructs correct subprocess command invoking transcribe_project.py with scene subfolder as --project"
    - "collect_scene_transcript() copies transcript from legacy flat path to canonical Transcription/{scene}_transcript.json"
    - "Existing scene transcript is skipped when already present (idempotent re-run)"
    - "merge_transcripts() produces merged_transcript.json with scene_id and local timecodes on every word"
    - "Merged words preserve local start/end timecodes, never global"
  artifacts:
    - path: "scripts/02_transcribe/0201_transcribe_nested/0201_transcribe_nested.py"
      provides: "Scene orchestrator core functions"
      exports: ["detect_scenes", "transcribe_scene", "collect_scene_transcript"]
    - path: "scripts/02_transcribe/0201_transcribe_nested/merge_transcripts.py"
      provides: "Cross-scene transcript merger"
      exports: ["read_scene_words", "merge_transcripts"]
    - path: "scripts/02_transcribe/0201_transcribe_nested/tests/test_0201.py"
      provides: "Unit tests for scene orchestration"
      min_lines: 60
    - path: "scripts/02_transcribe/0201_transcribe_nested/tests/test_merge.py"
      provides: "Unit tests for merge logic"
      min_lines: 40
  key_links:
    - from: "0201_transcribe_nested.py"
      to: "transcribe_project.py"
      via: "subprocess.run with venv python"
      pattern: "subprocess\\.run.*transcribe_project"
    - from: "merge_transcripts.py"
      to: "{scene}_transcript.json"
      via: "json.load reading clips[].transcript_segments[].words_data[]"
      pattern: "words_data"
    - from: "collect_scene_transcript"
      to: "Transcription/{scene}_transcript.json"
      via: "shutil.copy2 from legacy path"
      pattern: "shutil\\.copy2"
---

<objective>
TDD implementation of scene orchestrator core functions and cross-scene transcript merger for nested multi-scene projects.

Purpose: Build the testable core that Phase 3 Plan 02 will wire into a CLI. Each scene is transcribed independently by the existing transcribe_project.py (called via subprocess), then a merge step combines all scene transcripts into merged_transcript.json with scene_id routing.

Output: 7 tested functions across two modules (0201_transcribe_nested.py + merge_transcripts.py), all unit tests green.
</objective>

<execution_context>
@/Users/romansergeev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/romansergeev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-transcribe/03-RESEARCH.md

<interfaces>
<!-- Key interfaces from existing codebase that executor needs -->

From scripts/02_transcribe/020101_transcribe/transcribe_project.py (lines 755-834):
- When --project points at a scene subfolder (e.g. Source/Video/apartment/):
  - v3_structure = False (no 01_Media/Source/Video/ inside apartment/)
  - project_name = "apartment" (input_path.name)
  - transcription_dir = apartment/apartment_transcription/
  - Output: apartment/apartment_transcription/apartment_transcript.json
- CLI: --project PATH, -n NUM, -y, --dry-run, --stages 3,4,5

From scripts/02_transcribe/020101_transcribe/020101_transcribe_spec.md:
- transcript.json structure: { "project": str, "clips": [{ "clip_id": str, "transcript_segments": [{ "words_data": [{ "word": str, "start": float, "end": float, "duration": float, "confidence": float }], "speaker": str }] }] }

From 03-RESEARCH.md merged_transcript.json schema:
```json
{
  "version": "1.0",
  "project": "YTCR01_Arty_Dzis",
  "scenes": ["volleyball", "apartment", ...],
  "words": [
    { "scene_id": "volleyball", "word": "hello", "start": 1.46, "end": 1.82, "duration": 0.36, "confidence": 0.987, "speaker": "SPEAKER_00" }
  ]
}
```

Project venv path: ~/YTAI/environment/.venv_transcribe/bin/python
Script path: ~/YTAI/scripts/02_transcribe/020101_transcribe/transcribe_project.py
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Test scaffold for scene orchestration + merge</name>
  <files>
    scripts/02_transcribe/0201_transcribe_nested/__init__.py,
    scripts/02_transcribe/0201_transcribe_nested/tests/__init__.py,
    scripts/02_transcribe/0201_transcribe_nested/tests/conftest.py,
    scripts/02_transcribe/0201_transcribe_nested/tests/test_0201.py,
    scripts/02_transcribe/0201_transcribe_nested/tests/test_merge.py
  </files>
  <read_first>
    scripts/01_prepare/0104_sync_audio_nested/tests/conftest.py,
    scripts/01_prepare/0104_sync_audio_nested/tests/test_0104.py,
    .planning/phases/03-transcribe/03-RESEARCH.md
  </read_first>
  <behavior>
    test_0201.py:
    - test_detect_scenes: Given a tmp_path with Source/Video/{volleyball,apartment,drive_home}/ each containing a .MP4 file, detect_scenes returns 3 sorted Path objects. Hidden dirs (.DS_Store) and non-dirs are excluded.
    - test_detect_scenes_empty: Given Source/Video/ with no subdirs, detect_scenes returns empty list.
    - test_transcribe_scene_cmd: Given a scene_dir Path and num_speakers=2, transcribe_scene(scene_dir, 2, dry_run=False) calls subprocess.run with args=[venv_python, script_path, "--project", str(scene_dir), "-n", "2", "-y"]. Mock subprocess.run, assert call args.
    - test_transcribe_scene_dry_run: When dry_run=True, the subprocess command includes "--dry-run".
    - test_collect_scene_transcript: Given scene_dir=tmp/Source/Video/apartment with apartment/apartment_transcription/apartment_transcript.json existing, collect_scene_transcript copies it to tmp/01_Media/Source/Transcription/apartment_transcript.json. Assert destination file exists and content matches.
    - test_skip_existing_transcript: Given Transcription/apartment_transcript.json already exists, should_transcribe_scene returns False.

    test_merge.py:
    - test_read_scene_words: Given a sample transcript JSON with 2 clips, 3 segments, 5 words total, read_scene_words("volleyball", transcript_path) returns 5 dicts each with scene_id="volleyball", word, start, end, duration, confidence, speaker.
    - test_merge_transcripts: Given 2 scene transcript files (volleyball with 3 words, apartment with 2 words), merge_transcripts produces merged_transcript.json with version="1.0", scenes=["apartment","volleyball"] (sorted), words list of 5 entries.
    - test_merged_word_fields: Each word in merged output has exactly these keys: scene_id, word, start, end, duration, confidence, speaker. start/end are local (same values as source).
    - test_merge_missing_scene: If one scene transcript file is missing, merge_transcripts still processes the existing ones without error.
  </behavior>
  <action>
    Create the package structure:
    - scripts/02_transcribe/0201_transcribe_nested/__init__.py (empty)
    - scripts/02_transcribe/0201_transcribe_nested/tests/__init__.py (empty)

    Create conftest.py with fixtures:
    - `fake_nested_project(tmp_path)`: Creates full nested project structure:
      - tmp_path/01_Media/Source/Video/volleyball/ with a dummy C5001.MP4 (1-byte file)
      - tmp_path/01_Media/Source/Video/apartment/ with a dummy C5040.MP4
      - tmp_path/01_Media/Source/Video/drive_home/ with a dummy C5300.MP4
      - tmp_path/01_Media/Source/Transcription/ (empty dir)
      - Returns tmp_path
    - `sample_transcript_json(tmp_path)`: Creates a valid {scene}_transcript.json with this exact structure:
      ```python
      {
          "project": "volleyball",
          "structure": {"work_dir": str(tmp_path)},
          "clips": [
              {
                  "clip_id": "C5001",
                  "filename": "C5001.MP4",
                  "duration": 45.0,
                  "offset": 0.0,
                  "transcript_segments": [
                      {
                          "speaker": "SPEAKER_00",
                          "words_data": [
                              {"word": "hello", "start": 1.46, "end": 1.82, "duration": 0.36, "confidence": 0.987},
                              {"word": "world", "start": 1.90, "end": 2.30, "duration": 0.40, "confidence": 0.954}
                          ]
                      }
                  ]
              },
              {
                  "clip_id": "C5002",
                  "filename": "C5002.MP4",
                  "duration": 30.0,
                  "offset": 45.0,
                  "transcript_segments": [
                      {
                          "speaker": "SPEAKER_01",
                          "words_data": [
                              {"word": "test", "start": 0.50, "end": 0.80, "duration": 0.30, "confidence": 0.912}
                          ]
                      }
                  ]
              }
          ]
      }
      ```
      Writes to tmp_path/volleyball_transcript.json, returns path.

    Create test_0201.py using importlib.util.spec_from_file_location pattern (established project pattern from Phase 2). Use `@pytest.fixture(scope="module")` to defer module load:
    ```python
    import pytest
    import importlib.util
    from pathlib import Path

    @pytest.fixture(scope="module")
    def mod():
        spec = importlib.util.spec_from_file_location(
            "transcribe_nested",
            Path(__file__).resolve().parent.parent / "0201_transcribe_nested.py",
        )
        m = importlib.util.module_from_spec(spec)
        spec.loader.exec_module(m)
        return m
    ```

    Write 6 tests as described in behavior section. Each test takes `mod` fixture + conftest fixtures as needed. For subprocess tests, use `unittest.mock.patch("subprocess.run")`.

    Create test_merge.py with same importlib pattern loading merge_transcripts.py. Write 4 tests as described in behavior section.

    Run: `python3 -m pytest scripts/02_transcribe/0201_transcribe_nested/tests/ --collect-only` must exit 0 (tests collected).
    Run: `python3 -m pytest scripts/02_transcribe/0201_transcribe_nested/tests/ -x -q` must FAIL (RED — no implementation yet).
  </action>
  <verify>
    <automated>cd /Users/romansergeev/YTAI && python3 -m pytest scripts/02_transcribe/0201_transcribe_nested/tests/ --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - scripts/02_transcribe/0201_transcribe_nested/__init__.py exists (empty)
    - scripts/02_transcribe/0201_transcribe_nested/tests/__init__.py exists (empty)
    - scripts/02_transcribe/0201_transcribe_nested/tests/conftest.py contains `def fake_nested_project` and `def sample_transcript_json`
    - test_0201.py contains `def test_detect_scenes`, `def test_detect_scenes_empty`, `def test_transcribe_scene_cmd`, `def test_transcribe_scene_dry_run`, `def test_collect_scene_transcript`, `def test_skip_existing_transcript`
    - test_merge.py contains `def test_read_scene_words`, `def test_merge_transcripts`, `def test_merged_word_fields`, `def test_merge_missing_scene`
    - `pytest --collect-only` exits 0 with 10 tests collected
    - `pytest -x -q` FAILS (RED phase — implementation files do not exist yet)
  </acceptance_criteria>
  <done>10 tests collected, all fail because implementation modules do not exist yet (RED phase complete)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implement scene orchestrator + merge modules</name>
  <files>
    scripts/02_transcribe/0201_transcribe_nested/0201_transcribe_nested.py,
    scripts/02_transcribe/0201_transcribe_nested/merge_transcripts.py
  </files>
  <read_first>
    scripts/02_transcribe/0201_transcribe_nested/tests/test_0201.py,
    scripts/02_transcribe/0201_transcribe_nested/tests/test_merge.py,
    scripts/02_transcribe/0201_transcribe_nested/tests/conftest.py,
    .planning/phases/03-transcribe/03-RESEARCH.md
  </read_first>
  <action>
    Create 0201_transcribe_nested.py with module docstring, imports (pathlib, subprocess, shutil, argparse, json), and these functions:

    1. `detect_scenes(project: Path) -> list[Path]`:
       - video_dir = project / "01_Media" / "Source" / "Video"
       - Return sorted list of subdirs that are directories and do not start with "."
       - Do NOT require numeric prefix (consistent with Phase 1+2 scene detection)

    2. `should_transcribe_scene(project: Path, scene_name: str) -> bool`:
       - Check if project / "01_Media" / "Source" / "Transcription" / f"{scene_name}_transcript.json" exists
       - Return True if it does NOT exist (scene needs transcription)

    3. `transcribe_scene(scene_dir: Path, num_speakers: int, dry_run: bool = False) -> None`:
       - venv_python = Path("~/YTAI/environment/.venv_transcribe/bin/python").expanduser()
       - script = Path("~/YTAI/scripts/02_transcribe/020101_transcribe/transcribe_project.py").expanduser()
       - cmd = [str(venv_python), str(script), "--project", str(scene_dir), "-n", str(num_speakers), "-y"]
       - If dry_run: cmd.append("--dry-run")
       - subprocess.run(cmd, check=True)

    4. `collect_scene_transcript(scene_dir: Path, project: Path) -> Path`:
       - scene_name = scene_dir.name
       - src = scene_dir / f"{scene_name}_transcription" / f"{scene_name}_transcript.json"
       - dst_dir = project / "01_Media" / "Source" / "Transcription"
       - dst_dir.mkdir(parents=True, exist_ok=True)
       - dst = dst_dir / f"{scene_name}_transcript.json"
       - shutil.copy2(src, dst)
       - Return dst

    Create merge_transcripts.py with module docstring, imports (json, pathlib), and these functions:

    5. `read_scene_words(transcript_path: Path, scene_id: str) -> list[dict]`:
       - Load JSON from transcript_path
       - Iterate data["clips"] -> clip["transcript_segments"] -> segment["words_data"]
       - For each word, build dict: scene_id, word["word"], word["start"], word["end"], word.get("duration", end-start), word.get("confidence", 0.0), segment.get("speaker", "UNKNOWN")
       - Return list of word dicts

    6. `merge_transcripts(project: Path, scene_names: list[str]) -> Path`:
       - tr_dir = project / "01_Media" / "Source" / "Transcription"
       - For each scene_name in scene_names, check if tr_dir / f"{scene_name}_transcript.json" exists; if so, call read_scene_words
       - Build merged dict: {"version": "1.0", "project": project.name, "scenes": sorted(scene_names), "words": all_words}
       - Write to tr_dir / "merged_transcript.json" with json.dump(indent=2, ensure_ascii=False)
       - Return output path

    Run all tests: `python3 -m pytest scripts/02_transcribe/0201_transcribe_nested/tests/ -x -q`
    All 10 tests must pass (GREEN).
  </action>
  <verify>
    <automated>cd /Users/romansergeev/YTAI && python3 -m pytest scripts/02_transcribe/0201_transcribe_nested/tests/ -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - 0201_transcribe_nested.py contains `def detect_scenes(`, `def should_transcribe_scene(`, `def transcribe_scene(`, `def collect_scene_transcript(`
    - merge_transcripts.py contains `def read_scene_words(`, `def merge_transcripts(`
    - 0201_transcribe_nested.py contains `subprocess.run` for invoking transcribe_project.py
    - 0201_transcribe_nested.py contains `shutil.copy2` for collecting scene transcript
    - merge_transcripts.py contains `"scene_id"` in the word dict construction
    - merge_transcripts.py contains `"version": "1.0"` in the merged output
    - `python3 -m pytest scripts/02_transcribe/0201_transcribe_nested/tests/ -v` shows 10 passed, 0 failed
  </acceptance_criteria>
  <done>All 10 unit tests pass. detect_scenes, transcribe_scene, collect_scene_transcript, should_transcribe_scene, read_scene_words, merge_transcripts all implemented and tested.</done>
</task>

</tasks>

<verification>
All tests pass:
```bash
python3 -m pytest scripts/02_transcribe/0201_transcribe_nested/tests/ -v
```
Expected: 10 passed
</verification>

<success_criteria>
- 10 unit tests pass covering TRN-01 (scene detection, subprocess invocation, skip existing, output collection), TRN-02 (per-scene transcript path), TRN-03 (merge with scene_id + local timecodes)
- No modifications to existing transcribe_project.py or ingest_json.py
- Module importable via importlib.util for Plan 02 CLI orchestration
</success_criteria>

<output>
After completion, create `.planning/phases/03-transcribe/03-01-SUMMARY.md`
</output>
