---
phase: 01-organize
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - scripts/01_prepare/0100_organize/0100_organize.py
autonomous: false
requirements: [ORG-01, ORG-02, ORG-03, ORG-04, ORG-05, ORG-06]

must_haves:
  truths:
    - "dry-run on reference project lists all 7 scenes with correct clip counts"
    - "dry-run shows TX01, TX02, TX02_2 WAV collection plan"
    - "dry-run shows XML sidecar destination with scene layer"
    - "Script exits 0 on dry-run without moving any files"
  artifacts:
    - path: "scripts/01_prepare/0100_organize/0100_organize.py"
      provides: "Polished organize script with logging and dry-run output"
  key_links:
    - from: "0100_organize.py"
      to: "/Volumes/RYA T7 Black/YTCR_1_Arty_Dzis"
      via: "--project argument"
      pattern: "--project"
---

<objective>
Run dry-run verification on the reference project and get human confirmation that organize output matches expectations.

Purpose: Validates that the organize script correctly handles the real-world reference project (7 scenes, 325 clips, 3 TX folders, GoPro subfolder) before any files are moved. This is the end-to-end gate for Phase 1.

Output: Verified dry-run output; human approval that organize logic is correct.
</objective>

<execution_context>
@/Users/romansergeev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/romansergeev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-organize/01-RESEARCH.md
@.planning/phases/01-organize/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add structured dry-run summary output to organize script</name>
  <files>scripts/01_prepare/0100_organize/0100_organize.py</files>
  <read_first>
    scripts/01_prepare/0100_organize/0100_organize.py (current full state from Plan 02),
    .planning/phases/01-organize/01-RESEARCH.md (reference project details: 7 scenes, clip counts)
  </read_first>
  <action>
Add a `print_dry_run_summary()` function to 0100_organize.py that runs when `--dry-run` is active. It should print a structured summary BEFORE any moves showing:

1. **Project type:** "Nested project detected" or "Flat project"
2. **Scenes found:** List each scene name with count of video files (recursive) and whether it has subfolders (like 100GOPRO/)
3. **TX folders found:** List each TX folder with count of WAV files matching DJI_RAW_RE
4. **XML sidecars found:** List each XML with its target destination (per_clip/{scene}/{clip}/)
5. **v3.0 skeleton:** Which dirs would be created (only missing ones)
6. **Total moves:** X video files, Y WAV files, Z XML sidecars

Format example:
```
=== DRY RUN: 0100_organize ===
Project: /Volumes/RYA T7 Black/YTCR_1_Arty_Dzis
Type: Nested project (TX folders detected)

Scenes (7):
  volleyball        114 clips
  dubai_driving      51 clips
  desert_drive       44 clips
  apartment          40 clips
  al_qudra_lake      34 clips (has subfolders: 100GOPRO/)
  al_qudra_lake_story 35 clips
  drive_home          7 clips

TX folders (3):
  TX01/    3 WAV files -> 99_Pipeline/DJI_Audio/
  TX02/    9 WAV files -> 99_Pipeline/DJI_Audio/
  TX02_2/  4 WAV files -> 99_Pipeline/DJI_Audio/

XML sidecars: {count}
  {xml_name} -> per_clip/{scene}/{clip}/

Total: {N} video + {M} WAV + {K} XML = {total} file moves
=== END DRY RUN ===
```

Update `organize()` to call `print_dry_run_summary()` when `dry_run=True`, then return True without moving.

Also ensure that when `dry_run=False`, each move is logged with source and destination.
  </action>
  <verify>
    <automated>cd /Users/romansergeev/YTAI && python -m pytest scripts/01_prepare/0100_organize/tests/test_organize.py -x -q 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - 0100_organize.py contains "def print_dry_run_summary"
    - 0100_organize.py contains "DRY RUN" string in the summary output
    - 0100_organize.py dry_run path returns True without calling shutil.move
    - All 10 existing tests still pass
  </acceptance_criteria>
  <done>Dry-run mode prints structured summary of planned operations; all existing tests pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify dry-run output on reference project YTCR_1_Arty_Dzis</name>
  <files>scripts/01_prepare/0100_organize/0100_organize.py</files>
  <action>
Run the organize script in dry-run mode against the reference project on the external drive. Capture the output and present it for human verification. The command:

```bash
cd /Users/romansergeev/YTAI
python scripts/01_prepare/0100_organize/0100_organize.py \
  --project "/Volumes/RYA T7 Black/YTCR_1_Arty_Dzis" \
  --dry-run
```

If the drive is not mounted, inform the user and wait. After running, confirm:
- Exit code is 0
- Output shows 7 scenes
- Output shows 3 TX folders
- No files were moved (verify volleyball/ still has MP4s at original location)
  </action>
  <verify>
    <automated>cd /Users/romansergeev/YTAI && python scripts/01_prepare/0100_organize/0100_organize.py --help 2>&1 | grep -q "dry-run" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Dry-run output shows "Nested project" detection
    - Dry-run output lists 7 scene folders
    - Dry-run output lists TX01, TX02, TX02_2 folders with WAV counts
    - Exit code is 0
    - No files moved on the reference project
  </acceptance_criteria>
  <done>Human approves dry-run output as correct; Phase 1 organize script validated against real data</done>
</task>

</tasks>

<verification>
- `python scripts/01_prepare/0100_organize/0100_organize.py --project "/Volumes/RYA T7 Black/YTCR_1_Arty_Dzis" --dry-run` exits 0
- Output lists 7 scenes and 3 TX folders
- No files moved (dry-run only)
- Human confirms output matches expectations
</verification>

<success_criteria>
- Dry-run summary correctly identifies all 7 scenes, all 3 TX folders, and XML sidecars
- Clip counts in dry-run output approximately match known reference project totals
- Human approves that the organize plan is correct before real files are moved
- All 10 unit tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/01-organize/01-03-SUMMARY.md`
</output>
