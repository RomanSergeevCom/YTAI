---
phase: 01-organize
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - scripts/01_prepare/0100_organize/0100_organize.py
  - scripts/01_prepare/0100_organize/tests/test_organize.py
autonomous: true
requirements: [ORG-02, ORG-03, ORG-04]

must_haves:
  truths:
    - "Each scene's MP4/MOV clips land in 01_Media/Source/Video/{scene}/"
    - "GoPro subfolder structure inside al_qudra_lake is preserved"
    - "All TX WAV files land flat in 99_Pipeline/DJI_Audio/ with original names"
    - "Sony XML sidecars land in Transcription/per_clip/{scene}/{clip}/"
    - "Running organize twice does not error or duplicate files"
  artifacts:
    - path: "scripts/01_prepare/0100_organize/0100_organize.py"
      provides: "Complete organize script with file move logic"
      exports: ["move_scene_clips", "move_dji_wavs", "move_xml_sidecars", "build_clip_scene_map"]
  key_links:
    - from: "0100_organize.py::move_scene_clips"
      to: "01_Media/Source/Video/{scene}/"
      via: "shutil.move with relative_to for GoPro preservation"
      pattern: "clip\\.relative_to\\(scene_dir\\)"
    - from: "0100_organize.py::move_dji_wavs"
      to: "99_Pipeline/DJI_Audio/"
      via: "shutil.move flat"
      pattern: "dji_dir.*\\.name"
    - from: "0100_organize.py::move_xml_sidecars"
      to: "Transcription/per_clip/{scene}/{clip}/"
      via: "clip_scene_map lookup"
      pattern: "per_clip.*scene.*clip_id"
---

<objective>
Implement all file-move logic in 0100_organize.py: video clips per scene, DJI WAVs flat, XML sidecars with scene layer.

Purpose: After this plan, `0100_organize.py --project PATH` fully arranges a nested project into v3.0 structure. All 10 tests pass.

Output: Complete `0100_organize.py` with all ORG-02, ORG-03, ORG-04 requirements implemented and tested.
</objective>

<execution_context>
@/Users/romansergeev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/romansergeev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-organize/01-RESEARCH.md
@.planning/phases/01-organize/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output — functions already in 0100_organize.py -->
```python
def is_nested_project(project: Path) -> bool: ...
def detect_scenes(project: Path) -> list[str]: ...
def create_v3_skeleton(project: Path, template_dir: Path, dry_run: bool = False) -> int: ...
def organize(project: Path, dry_run: bool = False) -> bool: ...
```

<!-- From run_pipeline.py — existing move patterns to follow -->
From scripts/run_pipeline.py (lines 531-558):
```python
# Video move with scene preservation
for f in discovered["videos"]:
    scene = _get_scene_name(f, project)
    if scene:
        dest = video_dir / scene
    else:
        dest = video_dir
    dest.mkdir(parents=True, exist_ok=True)
    dst = dest / f.name
    if dst.exists():
        log.warn(f"Already exists, skip: {f.name}")
        continue
    shutil.move(str(f), str(dst))
```

From scripts/run_pipeline.py (lines 626-670):
```python
# XML sidecar move — current flat path (no scene layer)
for xml_f in discovered["sidecars"]:
    clip_id = _xml_to_clip_id(xml_f, video_stems)
    if clip_id:
        per_clip = tr_dir / "per_clip" / clip_id   # MISSING scene layer
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement video clip moves with scene preservation and GoPro subfolder support</name>
  <files>
    scripts/01_prepare/0100_organize/0100_organize.py,
    scripts/01_prepare/0100_organize/tests/test_organize.py
  </files>
  <read_first>
    scripts/01_prepare/0100_organize/0100_organize.py (current state from Plan 01),
    scripts/01_prepare/0100_organize/tests/test_organize.py (current stubs),
    scripts/01_prepare/0100_organize/tests/conftest.py (fixtures),
    scripts/run_pipeline.py (lines 491-558 for organize_media_files video section)
  </read_first>
  <behavior>
    - test_scene_clips_moved: after organize(fake_nested_project), Source/Video/volleyball/C5210.MP4 exists and Source/Video/apartment/C5089.MP4 exists
    - test_gopro_subfolder_preserved: after organize(fake_nested_project), Source/Video/al_qudra_lake/100GOPRO/GX010001.MP4 exists (not flattened to al_qudra_lake/GX010001.MP4)
    - test_dji_wavs_moved_flat: after organize(fake_nested_project), 99_Pipeline/DJI_Audio/TX01_MIC001_20260228_102211_orig.wav exists AND 99_Pipeline/DJI_Audio/TX02_MIC024_20260228_100209_orig.wav exists AND 99_Pipeline/DJI_Audio/TX02_MIC033_20260302_162711_orig.wav exists
  </behavior>
  <action>
**Step 1: Fill in test bodies** in test_organize.py for test_scene_clips_moved, test_gopro_subfolder_preserved, test_dji_wavs_moved_flat:

For each test:
- Call `organize(project_path)` (the function from 0100_organize)
- Assert destination files exist using `(project_path / "01_Media/Source/Video/volleyball/C5210.MP4").exists()`
- Assert source files no longer exist at original location

**Step 2: Run tests — they must FAIL (RED).**

**Step 3: Add functions to 0100_organize.py:**

1. `move_scene_clips(scenes: list[str], project: Path, video_dir: Path, dry_run: bool = False) -> bool`:
   - For each scene name in `scenes`:
     - `scene_dir = project / scene`
     - Recursively find all files with suffix in VIDEO_EXTS
     - For each clip: `rel = clip.relative_to(scene_dir)` → `dest = video_dir / scene / rel`
     - `dest.parent.mkdir(parents=True, exist_ok=True)`
     - Check `dest.exists()` → skip with print warning
     - `shutil.move(str(clip), str(dest))`
   - Return True if no errors

2. `move_dji_wavs(project: Path, dji_dir: Path, dry_run: bool = False) -> bool`:
   - Find all dirs at project root matching `TX_FOLDER_RE`
   - For each TX dir, find all `.wav` files matching `DJI_RAW_RE`
   - Move each WAV flat to `dji_dir / wav.name`
   - Check `dest.exists()` → skip
   - Return True if no errors

3. **Update `organize()` function** to call `move_scene_clips()` and `move_dji_wavs()` after `create_v3_skeleton()`. The flow:
   ```python
   scenes = detect_scenes(project)
   video_dir = project / "01_Media" / "Source" / "Video"
   dji_dir = project / "99_Pipeline" / "DJI_Audio"
   move_scene_clips(scenes, project, video_dir, dry_run)
   move_dji_wavs(project, dji_dir, dry_run)
   ```

4. Add cleanup: after all moves, remove empty TX directories and empty scene directories at project root. Use `rmdir()` which only removes empty dirs (safe).

**Step 4: Run tests — they must PASS (GREEN).**
  </action>
  <verify>
    <automated>cd /Users/romansergeev/YTAI && python -m pytest scripts/01_prepare/0100_organize/tests/test_organize.py::TestVideoMove scripts/01_prepare/0100_organize/tests/test_organize.py::TestDjiWavMove -x -q 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - 0100_organize.py contains "def move_scene_clips(scenes: list[str], project: Path"
    - 0100_organize.py contains "def move_dji_wavs(project: Path"
    - 0100_organize.py contains "clip.relative_to(scene_dir)" for GoPro subfolder preservation
    - 0100_organize.py contains "DJI_RAW_RE.match" for WAV filename validation
    - 0100_organize.py contains "dst.exists()" check before each move (idempotency)
    - test_scene_clips_moved asserts "01_Media/Source/Video/volleyball/C5210.MP4" exists
    - test_gopro_subfolder_preserved asserts "01_Media/Source/Video/al_qudra_lake/100GOPRO/GX010001.MP4" exists
    - test_dji_wavs_moved_flat asserts "99_Pipeline/DJI_Audio/TX01_MIC001_20260228_102211_orig.wav" exists
    - `python -m pytest tests/test_organize.py::TestVideoMove tests/test_organize.py::TestDjiWavMove -x` passes (3 tests green)
  </acceptance_criteria>
  <done>Video clips move per-scene preserving GoPro subfolders; DJI WAVs move flat to DJI_Audio; 3 file-move tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement XML sidecar moves with scene layer and finalize organize()</name>
  <files>
    scripts/01_prepare/0100_organize/0100_organize.py,
    scripts/01_prepare/0100_organize/tests/test_organize.py
  </files>
  <read_first>
    scripts/01_prepare/0100_organize/0100_organize.py (current state after Task 1),
    scripts/01_prepare/0100_organize/tests/test_organize.py (current state after Task 1),
    scripts/01_prepare/0100_organize/tests/conftest.py (fixtures — note which XML files exist),
    scripts/run_pipeline.py (lines 626-670 for XML sidecar logic, _xml_to_clip_id pattern)
  </read_first>
  <behavior>
    - test_xml_sidecar_with_scene: after organize(fake_nested_project), per_clip/volleyball/C5089/C5089M01.XML exists (not per_clip/C5089/C5089M01.XML)
    - Full suite: all 10 tests pass
  </behavior>
  <action>
**Step 1: Fill in test body** for test_xml_sidecar_with_scene:
- Call `organize(project_path)`
- Assert `(project / "01_Media/Source/Transcription/per_clip/volleyball/C5089/C5089M01.XML").exists()` is True
- Assert that the old sidecar path `(project / "01_Media/Source/Transcription/per_clip/C5089/C5089M01.XML")` does NOT exist (confirming scene layer added)
- Note: conftest creates `volleyball/C5089M01.XML` — the XML name starts with C5089 which matches clip C5089 in volleyball scene.

**Step 2: Run test — must FAIL (RED).**

**Step 3: Add functions to 0100_organize.py:**

1. `build_clip_scene_map(scenes: list[str], project: Path) -> dict[str, str]`:
   - For each scene, for each video file in `project / scene` (rglob), add `{clip.stem: scene}` to map
   - Returns dict mapping clip stem (e.g. "C5089") to scene name (e.g. "volleyball")

2. `xml_to_clip_id(xml_path: Path, video_stems: set[str]) -> str | None`:
   - Sony XML naming: `C5089M01.XML` — the clip ID is the stem minus the suffix marker
   - Try progressively shorter prefixes of xml stem until match found in video_stems
   - For `C5089M01`: try `C5089M01` (no match), then `C5089M0` (no match), then `C5089M` (no match), then `C5089` (match) → return "C5089"
   - If no match found, return None

3. `move_xml_sidecars(project: Path, scenes: list[str], tr_dir: Path, dry_run: bool = False) -> bool`:
   - Build clip_scene_map from scenes
   - Collect video_stems from all scene dirs
   - Find all XML files in non-managed, non-system dirs at project root (in scene dirs, since that's where they start)
   - For each XML: get clip_id via xml_to_clip_id, look up scene from clip_scene_map
   - Destination: `tr_dir / "per_clip" / scene / clip_id / xml.name` (with scene layer!)
   - If no scene found, fall back to `tr_dir / "per_clip" / clip_id / xml.name` (flat, backward compat)
   - Check `dst.exists()` → skip
   - `shutil.move(str(xml), str(dst))`

4. **Update `organize()` function** to call `move_xml_sidecars()`:
   ```python
   tr_dir = project / "01_Media" / "Source" / "Transcription"
   move_xml_sidecars(project, scenes, tr_dir, dry_run)
   ```

5. **Add summary output** at end of `organize()`: print count of scenes found, clips moved, WAVs moved, XMLs moved.

**Step 4: Run full test suite — all 10 must PASS (GREEN).**
  </action>
  <verify>
    <automated>cd /Users/romansergeev/YTAI && python -m pytest scripts/01_prepare/0100_organize/tests/test_organize.py -v 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - 0100_organize.py contains "def build_clip_scene_map(scenes: list[str], project: Path) -> dict"
    - 0100_organize.py contains "def xml_to_clip_id(xml_path: Path, video_stems: set"
    - 0100_organize.py contains "def move_xml_sidecars(project: Path, scenes: list[str]"
    - 0100_organize.py XML move path contains "per_clip" / scene / clip_id pattern (grep: "per_clip.*scene")
    - test_xml_sidecar_with_scene asserts path contains "per_clip/volleyball/C5089"
    - `python -m pytest scripts/01_prepare/0100_organize/tests/ -v` shows 10 passed, 0 failed
    - `python scripts/01_prepare/0100_organize/0100_organize.py --help` exits 0 and shows --project and --dry-run
  </acceptance_criteria>
  <done>XML sidecars move to per_clip/{scene}/{clip}/; all 10 tests pass; organize script fully functional</done>
</task>

</tasks>

<verification>
- `python -m pytest scripts/01_prepare/0100_organize/tests/ -v` — 10 tests pass
- `python scripts/01_prepare/0100_organize/0100_organize.py --help` — shows usage
- `grep -c "shutil.move" scripts/01_prepare/0100_organize/0100_organize.py` — at least 3 (video, wav, xml)
- `grep "relative_to" scripts/01_prepare/0100_organize/0100_organize.py` — confirms GoPro subfolder preservation
- `grep "per_clip" scripts/01_prepare/0100_organize/0100_organize.py` — confirms scene layer in XML path
</verification>

<success_criteria>
- All MP4/MOV clips from each scene land in Source/Video/{scene}/ preserving internal subfolder structure
- All DJI WAVs from TX01/, TX02/, TX02_2/ land flat in 99_Pipeline/DJI_Audio/
- All XML sidecars land in Transcription/per_clip/{scene}/{clip}/ with the scene layer
- 10/10 tests pass
- Script has --project and --dry-run CLI arguments
- Idempotent: running twice skips already-moved files
</success_criteria>

<output>
After completion, create `.planning/phases/01-organize/01-02-SUMMARY.md`
</output>
